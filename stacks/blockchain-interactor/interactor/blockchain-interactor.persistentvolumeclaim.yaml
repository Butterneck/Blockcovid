apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blockchain-interactor-claim
  namespace: blockchain-interactor
  labels:
    app: blockchain-interactor-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
