_format_version: "2.1"
_transform: true

services:
- host: blockchain-interactor
  port: 3001
  protocol: http
  routes:
  - hosts:
    paths:
    - /api/blockchain-interactor
    strip_path: true

- host: inventory
  port: 3002
  protocol: http
  routes:
  - hosts:
    paths:
    - /api/inventory
    strip_path: true

- host: identities
  port: 3003
  protocol: http
  routes:
  - hosts:
    paths:
    - /api/identities
    strip_path: true

- host: booking
  port: 3004
  protocol: http
  routes:
  - hosts:
    paths:
    - /api/booking
    strip_path: true

- host: web-interface
  port: 4200
  protocol: http
  routes:
  - hosts:
    paths:
    - /

- host: message-broker
  port: 15672
  protocol: http
  routes:
  - hosts:
    paths:
    - /internals/message-broker
    strip_path: true